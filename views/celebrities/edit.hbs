<div class="edit-page">
<h2>Edit {{selectedCelebrity.name}}</h2>
<form action="/celebrities/{{selectedCelebrity.id}}/edit" method="POST">
    <label>
        Name:
        <input type="text" name="name" value="{{selectedCelebrity.name}}">
    </label>

    <label>
        Occupation:
        <input type="text" name="occupation" value="{{selectedCelebrity.occupation}}">
    </label>

    <label>
        Catch Phrase:
        <input type="text" name="catchPhrase" value="{{selectedCelebrity.catchPhrase}}">
    </label>

    <button type="submit"> Save editing</button>
</form>
</div>