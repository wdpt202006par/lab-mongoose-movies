<div class="movie-detail-page">
    <h2>{{movie.title}}</h2>
    <p>Genre: {{movie.genre}}</p>
    <p>Plot: {{movie.plot}}</p>
    <p>Cast: 
        {{!-- {{movie.cast.0.name}} --}}

        {{#each movie.cast}}
            {{name}}
        {{/each}}
        
    </p>
</div>